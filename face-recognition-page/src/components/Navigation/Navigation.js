import React from "react";

const Navigation = () => {
    return(
      <nav style={{display: 'flex', justifyContent:'flex-end', zIndex: '2'}}>
          <p className="relative f3 link dim black underline pa3 pointer Gumball">Sign Out</p>
      </nav>  
    );
}

export default Navigation